"use strict";

function addAddress() {
	let table = document.getElementById("addressCheck");
	let addAddressButton = document.createElement("button");
	//addAddressButton.onclick = function() {del(newRow, price);}